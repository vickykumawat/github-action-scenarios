name: Scenario-02
on:
 push:
 #workflow_dispatch:
    branches:
      - main
    paths-ignore:
      - 'docs/**'
      - 'README.md'
jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: say hello
        run: echo "Hello, World!"
  